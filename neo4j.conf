# Neo4j 5.x Minimal Configuration for Replit

# Essential settings only
dbms.default_database=neo4j
dbms.directories.data=./data
dbms.directories.logs=./logs
dbms.directories.plugins=./plugins

# Minimal memory settings for Replit
dbms.memory.heap.initial_size=128m
dbms.memory.heap.max_size=256m
dbms.memory.pagecache.size=128m

# Network settings
dbms.connector.bolt.enabled=true
dbms.connector.bolt.listen_address=0.0.0.0:7687
dbms.connector.http.enabled=true
dbms.connector.http.listen_address=0.0.0.0:7474

# Basic security
dbms.security.auth_enabled=true

# Essential procedures only
dbms.security.procedures.unrestricted=apoc.coll.*,apoc.load.*,apoc.meta.*,apoc.node.*
